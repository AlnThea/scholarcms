# 2026-09-07

### ♻️ Refactoring & Optimasi Kode
- **Pemisahan Komponen Post (Aturan 100-300 baris)**: Memulai refactoring pada `src/app/post/[slug]/page.jsx` (diubah namanya menjadi `PostClient.jsx`) untuk beralih ke Server-Side Rendering (menyelesaikan isu Crawled - currently not indexed pada Google Search Console).
- Mengekstrak bagian komentar pengunjung ke komponen baru `src/components/blog/PostComments.jsx` untuk memangkas ukuran baris kode file utama (menjadi 487 baris).
- Memulai refactoring pada `src/app/dashboard/page.jsx` dengan mengekstrak `DualLineComparisonWidget` dan `PostStatusStackedWidget` ke komponen terpisah untuk secara bertahap mengurangi file raksasa (2.000+ baris) sesuai aturan batas maksimal.

### 🛠️ Perbaikan Bug
- **Bug SEO Canonical Tag**: Memperbaiki `layout.jsx` pada halaman artikel untuk memastikan tag `canonical` selalu ter-generate menuju ke URL spesifik artikel (misal: `/post/[slug]`) meskipun server lambat. Menambahkan mekanisme auto-strip *double-slash* dari `NEXT_PUBLIC_SITE_URL` untuk menuntaskan pesan error AdSense & GSC "Alternate page with proper canonical tag".
