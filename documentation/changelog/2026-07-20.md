# Daily Changelog – 2026-07-20

- Added `documentation/INSTALL.md` with installation steps.
- Added `documentation/STRUCTURE.md` describing project layout.
- Added `documentation/DATABASE.md` with Firestore schema.
- Added `documentation/CHANGELOG.md` (main changelog) and updated it to reference daily logs.
- Created folder `documentation/changelog/` to store daily logs.
- Enhanced Firebase Authentication error handling in `src/services/authService.js` (human-readable error messages for `auth/configuration-not-found` & `permission-denied`).
- Updated `src/context/AuthContext.jsx` with real-time `onAuthStateChanged` listener.
- Implemented Route Protection (Auth Guard) in `src/app/admin/layout.jsx` redirecting unauthenticated users to `/login`.
- Removed fallback dummy administrator (`admin@scholarcms.com`) and Demo Login buttons from `/login`.
- Updated `/register` flow: automatically assigns `role: 'user'` to new accounts, while auto-detecting the first registered user in Firestore to assign `role: 'admin'`.
- Created `src/context/ThemeContext.jsx` for persistent Dark/Light theme switching stored in `localStorage` (`scholarcms_theme`) and synced across Navbar, Admin Header, and Root Layout.
- Upgraded styling engine from Tailwind CSS v3.4 to **Tailwind CSS v4.3.3** using `@tailwindcss/postcss` and `@import "tailwindcss"`.

*All entries correspond to code changes made on this date.*
